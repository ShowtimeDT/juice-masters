/** One-time reminder of the counting rule, shown once per scoreboard. */
export default function CountingNote() {
  return (
    <p className="text-right text-[10px] uppercase tracking-widest text-faint">
      Best 5 of 8 scores count
    </p>
  );
}
